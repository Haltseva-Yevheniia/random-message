import 'package:flutter/material.dart';

// Texts from HomePage
Text homePageAppBarTitle = const Text('Random Message');

Text homePageBodyMainText = const Text(
  'Let\'s try to make YOU HAPPY\n',
  textAlign: TextAlign.center,
  style: TextStyle(
      fontSize: 25,
      fontFamily: 'Pacifico',),
);

Text homePageBodyMainText2 = const Text(
  '\nPress the button below',
  style: TextStyle(
    fontSize: 20,
  ),
);

Text homePageButtonText = const Text('GO');

//Texts from Message Page
Text messagePageAppBarTitle = const Text('Random Message');
